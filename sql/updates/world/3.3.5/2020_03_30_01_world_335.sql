CREATE TABLE `item_generator_displays` (
	`quality` INT UNSIGNED NOT NULL,
	`classType` INT UNSIGNED NOT NULL,
	`subclassType` INT UNSIGNED NOT NULL,
	`inventoryType` INT UNSIGNED NOT NULL,
	`displayId` INT UNSIGNED NOT NULL
)
COLLATE='latin1_swedish_ci'
;
